import Nimble
import Quick

@testable import Zomavan

final class ZomavanTests: QuickSpec {

    override func spec() {
     
        describe("a test") {
            
            it("passes") {
                expect(true).to(beTruthy())
            }
        }
    }
}
